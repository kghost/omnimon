file(GLOB omnimon_srcs LIST_DIRECTORIES false "./*.cpp" "./*.hpp")
omnimon_add_executable(omnimon ${omnimon_srcs})

omnimon_target_link_libraries(omnimon ${BPF_LIBRARIES})
omnimon_target_link_libraries(omnimon omnimon-backend-events)
omnimon_target_link_libraries(omnimon omnimon-backend-process)
omnimon_target_link_libraries(omnimon omnimon-backend-system)
omnimon_target_link_libraries(omnimon omnimon-backend-cgroupv2)
omnimon_target_link_libraries(omnimon omnimon-backend-network)
omnimon_target_link_libraries(omnimon process-file-io)
omnimon_target_link_libraries(omnimon ftxui::component)
omnimon_target_link_libraries(omnimon ${ICUUC_LIBRARIES})
omnimon_target_link_libraries(omnimon ${ICUI18N_LIBRARIES})

omnimon_target_include_directories(omnimon PUBLIC ${BPF_INCLUDE_DIRS})
omnimon_target_compile_options(omnimon PUBLIC ${BPF_CFLAGS_OTHER})
